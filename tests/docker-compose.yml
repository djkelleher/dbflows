services:
  postgres:
    container_name: postgres
    image: postgres:15
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test
  minio:
    container_name: minio
    image: minio/minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data
  dbgress_test:
    container_name: dbgress_test
    build:
      context: ..
      dockerfile: tests/Dockerfile
    depends_on:
      - postgres
      - minio
    command: pytest